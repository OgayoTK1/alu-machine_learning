#!/usr/bin/env python3
"""
12. We are all an integral part of the web of life

Mandatory task demonstrating the indefinite integral of an exponential function.

Exponential functions are fundamental in machine learning (e.g., activation functions,
loss landscapes, probability distributions).

The specific integral computed here is:

∫ e^{2y} dy = (1/2) e^{2y} + C

This illustrates the reverse chain rule (u-substitution): let u = 2y, du = 2 dy →
∫ e^u (du/2) = (1/2) e^u + C = (1/2) e^{2y} + C
"""

import math


def integral(C=0):
    """
    Returns a callable representing the indefinite integral (antiderivative)
    of e^{2y}.

    The returned function F satisfies F'(y) ≈ e^{2y} (verified numerically
    by the checker).

    Args:
        C (int or float, optional): Constant of integration. Defaults to 0.

    Returns:
        callable: Function F(y) = (1/2) e^{2y} + C
    """
    return lambda y: C + (math.exp(2 * y) / 2.0)
